language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm install
  - npm install jest-cli
  - npm test
env:
  global:
    - CI=true
deploy:
  provider: heroku
  api-key: $HEROKU_PRODUCTION_API_KEY
  app: thumbtack-clone
  on:
    branch: feature/ci_deployment